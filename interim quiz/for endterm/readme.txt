1. 2개의 정수의 합과 차를 동시에 반환하는 함수 get_sum_diff()를 작성하고 결과를 확인하라. (포인터 매개변수 사용)  

 

2. 정수 배열 A[]를 다른 정수 배열 B[]에 복사하는 함수 array_copy()를 작성하고 결과를 확인하라.(배열 A와 B는 포인터 매개변수, SIZE는 일반 매개변수 사용)



3. 직원들의 월급이 배열 A[]에 저장되어 있을때, 이번 달 회사에서 지급할 월급의 총액을 계산하고자 한다. 정수형 배열 요소들의 합을 구하여 반환하는 함수를 작성하고 겨로가를 확인하라. 함수의 원형은 아래와 같다.

   int array_sum(int *A, int size);



4. 사용자로부터 2개의 정수를 받아오는 함수를 작성하라.  함수의 원형은 아래와 같다.

   void get_int(int *px, int *py);



5. 문자열 안에 포함된 특정한 문자의 갯수를 세는 함수 int str_chr(char *s, int c)를 작성하라. 여기서 s는 문자열이고, c는 개수를 세어야 할 문자이다.



6. 다음과 같이 연산의 이름을 문자열로 받아서 해당 연산을 실행하는 프로그램을 작성하라. 연산을 나타내는 문자열은 "add", "sub", "mul", "div"로 한다.

    연산을 입력하라: add 5 10

    연산의 결과: 15



7. 2차원 평면에서 점은 (x, y)로 나타낸다. 하나의 점은 다음과 같은 구조체로 정의할 수 있다. 두개의 point 구조체를 받아서 일치하면 1을 반환하고, 그렇지 않으면 0을 반환하는 함수 int equal(struct point p1, struct point p2)를 작성하고 결과를 확인하라.  



8. 7번 문제에서 equal()함수를 다음과 같이 구조체의 포인터를 받도록 변경하여 작성하고 그 결과를 확인하라.

   int equal(struct point *p1, struct point *p2) 



9. 다음은 무엇을 선언하는 문장인지 설명하라.

    1) int **dp;

    2) int *pa[10];

    3) int (*p)[3];

    4) void (*f)(int a, int b);



10. int형 배열과 int형 포인터를 받아서 포인터가 가장 큰 값을 가리키게 하는 함수 set_max_ptr() 구현하고 결과를 확인하라.

    Hint) 

          int 형 배열:      

               int m[7] = {10, 30, 20, 50, 70, 90, 40}

          함수 호출 부분:   

               set_max_ptr(m, 7, &pmax);
